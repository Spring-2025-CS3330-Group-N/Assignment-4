package hw4.maze;

/**
 * Enum options for CellComponents members of the Cell class.
 * Aperture represents an opening in the cell that players can move across.
 * Wall represents a barrier that players cannot cross.
 * Exit represents the end of the maze.
 */
public enum CellComponents {
	APERTURE,
	EXIT,
	WALL,
}
